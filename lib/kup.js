// Generated by CoffeeScript 1.8.0
(function() {
  var Kup, contentEncodings, empty, regular, tag, _fn, _i, _j, _len, _len1, _results;
  Kup = function() {
    return this.htmlOut = '';
  };
  if (typeof window !== "undefined" && window !== null) {
    window.Kup = Kup;
  } else if ((typeof module !== "undefined" && module !== null ? module.exports : void 0) != null) {
    module.exports = Kup;
  } else {
    throw new Error('either the `window` global or the `module.exports` global must be present');
  }
  contentEncodings = {
    '&': '&amp;',
    '<': '&lt;',
    '>': '&gt;',
    '"': '&quot;',
    '\'': '&#x27;',
    '/': '&#x2F;'
  };
  Kup.prototype = {
    encodeContent: function(content) {
      return content.toString().replace(/[&<>"'\/]/g, function(char) {
        return contentEncodings[char];
      });
    },
    encodeAttribute: function(value) {
      return value.toString().replace(/"/g, '&quot;');
    },
    camelcaseToDashcase: function(string) {
      return string.replace(/([a-z])([A-Z])/g, '$1-$2').toLowerCase();
    },
    attributeToString: function(key, value) {
      if (key === 'style' && 'object' === typeof value) {
        value = this.styleObjectToString(value);
      }
      return "" + key + "=\"" + (this.encodeAttribute(value)) + "\"";
    },
    styleObjectToString: function(styles) {
      var key, parts, value;
      parts = [];
      for (key in styles) {
        value = styles[key];
        parts.push("" + (this.camelcaseToDashcase(key)) + ": " + value + ";");
      }
      return parts.join(' ');
    },
    openingTagUntilExceptClosingBracketToString: function(tag, attrs) {
      var key, parts, value;
      parts = ["<" + tag];
      for (key in attrs) {
        value = attrs[key];
        if (value == null) {
          throw new Error("value of attribute `" + key + "` in tag `" + tag + "` is undefined or null");
        }
        parts.push(this.attributeToString(key, value));
      }
      return parts.join(' ');
    },
    doctype: function() {
      return this.htmlOut += '<!DOCTYPE html>';
    },
    open: function(tag, attrs) {
      return this.htmlOut += this.openingTagUntilExceptClosingBracketToString(tag, attrs) + '>';
    },
    content: function(content) {
      var stringContent, type;
      type = typeof content;
      if (type === 'function') {
        return content();
      } else if (content != null) {
        stringContent = type !== 'string' ? content.toString() : content;
        return this.htmlOut += this.encodeContent(stringContent);
      }
    },
    close: function(tag) {
      return this.htmlOut += "</" + tag + ">";
    },
    tag: function(tag, attrs, content) {
      this.open(tag, attrs);
      this.content(content);
      return this.close(tag);
    },
    empty: function(tag, attrs) {
      return this.htmlOut += this.openingTagUntilExceptClosingBracketToString(tag, attrs) + ' />';
    },
    unsafe: function(string) {
      return this.htmlOut += string;
    },
    safe: function(string) {
      return this.htmlOut += this.encodeContent(string);
    }
  };
  regular = 'a abbr address article aside audio b bdi bdo blockquote body button canvas caption cite code colgroup datalist dd del details dfn div dl dt em fieldset figcaption figure footer form frameset h1 h2 h3 h4 h5 h6 head header hgroup html i iframe ins kbd label legend li map mark menu meter nav noscript object ol optgroup option output p pre progress q rp rt ruby s samp script section select small span strong sub summary sup table tbody td textarea tfoot th thead time title tr u ul video style'.split(/[\n ]+/);
  _fn = function(tag) {
    return Kup.prototype[tag] = function(attrs, content) {
      if ('object' !== typeof attrs) {
        content = attrs;
        attrs = void 0;
      }
      return this.tag(tag, attrs, content);
    };
  };
  for (_i = 0, _len = regular.length; _i < _len; _i++) {
    tag = regular[_i];
    _fn(tag);
  }
  empty = 'area base br col command embed hr img input keygen link meta param source track wbr frame'.split(/[\n ]+/);
  _results = [];
  for (_j = 0, _len1 = empty.length; _j < _len1; _j++) {
    tag = empty[_j];
    _results.push((function(tag) {
      return Kup.prototype[tag] = function(attrs, content) {
        if ('object' !== typeof attrs) {
          content = attrs;
          attrs = void 0;
        }
        if (content != null) {
          throw new Error("void tag `" + tag + "` accepts no content but content `" + content + "` was given");
        }
        return this.empty(tag, attrs, content);
      };
    })(tag));
  }
  return _results;
})();
